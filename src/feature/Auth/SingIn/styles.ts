import { lightColors } from "@shared/help/colors";
import { spacing } from "@shared/help/spacing";
import { StyleSheet } from "react-native";

export const stylesSingIn = StyleSheet.create({
    containerSigIn: {
        flexGrow: 1,
        backgroundColor: lightColors.purple800,
        padding: spacing[16],
        gap: spacing[16],
    },
});
